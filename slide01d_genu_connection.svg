<svg width="1920" height="1080" viewBox="0 0 1920 1080" xmlns="http://www.w3.org/2000/svg" style="background: #0f1419;">
  <defs>
    <linearGradient id="headerGradient01d" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" style="stop-color:#8B7BD8;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#A78BFA;stop-opacity:1" />
    </linearGradient>
    <style>
      .bg { fill: #0f1419; }
      .title { fill: #ffffff; font-family: 'Arial', sans-serif; font-size: 64px; font-weight: bold; }
      .label { fill: #a0aec0; font-family: 'Arial', sans-serif; font-size: 32px; }
      .value { fill: #ffffff; font-family: 'Arial', sans-serif; font-size: 36px; font-weight: bold; }
      .box { fill: #2d3748; stroke: #8B7BD8; stroke-width: 2; rx: 10; }
      .url { fill: #A78BFA; font-family: 'Arial', sans-serif; font-size: 32px; text-decoration: underline; }
      .copy-btn { cursor: pointer; }
      .copy-btn:hover rect { fill: #8B7BD8; }
    </style>
  </defs>
  
  <!-- Background -->
  <rect class="bg" width="1920" height="1080"/>
  
  <!-- Header bar -->
  <rect x="0" y="0" width="1920" height="120" fill="url(#headerGradient01d)"/>
  
  <!-- Title -->
  <text class="title" x="960" y="80" text-anchor="middle">GenU 接続情報</text>
  
  <!-- Connection URL Section -->
  <rect class="box" x="200" y="200" width="1520" height="120"/>
  <text class="label" x="240" y="250">URL:</text>
  <a href="https://d27hf1k0jfx5ie.cloudfront.net/" target="_blank">
    <text class="url" x="240" y="295" style="cursor: pointer;">https://d27hf1k0jfx5ie.cloudfront.net/</text>
  </a>
  
  <!-- Copy button for URL -->
  <g class="copy-btn" onclick="copyToClipboard('https://d27hf1k0jfx5ie.cloudfront.net/')">
    <rect x="1500" y="240" width="180" height="60" rx="8" fill="#1a202c" stroke="#8B7BD8" stroke-width="2"/>
    <text x="1590" y="275" text-anchor="middle" fill="#ffffff" font-family="Arial, sans-serif" font-size="24" font-weight="bold">
      📋 コピー
    </text>
  </g>
  
  <!-- Username Section -->
  <rect class="box" x="200" y="360" width="1520" height="130"/>
  <text class="label" x="240" y="415">ユーザ名:</text>
  <text class="value" x="550" y="415">handson-00XX-aws-linked@aws-networld.jp</text>
  <text class="label" x="550" y="455" font-size="28px">(座席番号+10)</text>
  
  <!-- Copy button for Username -->
  <g class="copy-btn" onclick="copyToClipboard('handson-00XX-aws-linked@aws-networld.jp')">
    <rect x="1500" y="390" width="180" height="60" rx="8" fill="#1a202c" stroke="#8B7BD8" stroke-width="2"/>
    <text x="1590" y="425" text-anchor="middle" fill="#ffffff" font-family="Arial, sans-serif" font-size="24" font-weight="bold">
      📋 コピー
    </text>
  </g>
  
  <!-- Password Section -->
  <rect class="box" x="200" y="530" width="1520" height="100"/>
  <text class="label" x="240" y="585">パスワード:</text>
  <text class="value" x="550" y="585">Networld02!</text>
  
  <!-- Copy button for Password -->
  <g class="copy-btn" onclick="copyToClipboard('Networld02!')">
    <rect x="1500" y="550" width="180" height="60" rx="8" fill="#1a202c" stroke="#8B7BD8" stroke-width="2"/>
    <text x="1590" y="585" text-anchor="middle" fill="#ffffff" font-family="Arial, sans-serif" font-size="24" font-weight="bold">
      📋 コピー
    </text>
  </g>
  
  <!-- Footer Note -->
  <text class="label" x="960" y="700" text-anchor="middle" font-size="28px">※ 接続に問題がある場合は、スタッフまでお声がけください</text>
  
  <script type="text/javascript">
    <![CDATA[
    function copyToClipboard(text) {
      if (navigator.clipboard && window.isSecureContext) {
        navigator.clipboard.writeText(text).then(function() {
          showCopyMessage();
        }).catch(function(err) {
          console.error('コピーに失敗しました: ', err);
        });
      } else {
        // フォールバック: テキストエリアを使用
        const textArea = document.createElement('textarea');
        textArea.value = text;
        document.body.appendChild(textArea);
        textArea.select();
        try {
          document.execCommand('copy');
          showCopyMessage();
        } catch (err) {
          console.error('コピーに失敗しました: ', err);
        }
        document.body.removeChild(textArea);
      }
    }
    
    function showCopyMessage() {
      // 既存のメッセージを削除
      const existingMessage = document.getElementById('copyMessage');
      if (existingMessage) {
        existingMessage.remove();
      }
      
      // コピー完了メッセージを表示
      const message = document.createElementNS('http://www.w3.org/2000/svg', 'g');
      message.setAttribute('id', 'copyMessage');
      
      const rect = document.createElementNS('http://www.w3.org/2000/svg', 'rect');
      rect.setAttribute('x', '810');
      rect.setAttribute('y', '400');
      rect.setAttribute('width', '300');
      rect.setAttribute('height', '80');
      rect.setAttribute('rx', '15');
      rect.setAttribute('fill', '#32cd32');
      rect.setAttribute('opacity', '0.95');
      
      const text = document.createElementNS('http://www.w3.org/2000/svg', 'text');
      text.setAttribute('x', '960');
      text.setAttribute('y', '450');
      text.setAttribute('text-anchor', 'middle');
      text.setAttribute('fill', '#ffffff');
      text.setAttribute('font-family', 'Arial, sans-serif');
      text.setAttribute('font-size', '32');
      text.setAttribute('font-weight', 'bold');
      text.textContent = '✓ コピーしました！';
      
      message.appendChild(rect);
      message.appendChild(text);
      document.querySelector('svg').appendChild(message);
      
      // 2秒後にメッセージを削除
      setTimeout(function() {
        const msg = document.getElementById('copyMessage');
        if (msg) {
          msg.remove();
        }
      }, 2000);
    }
    ]]>
  </script>
  
</svg>
