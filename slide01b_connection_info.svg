<svg width="1920" height="1080" viewBox="0 0 1920 1080" xmlns="http://www.w3.org/2000/svg" style="background: #0f1419;">
  <defs>
    <linearGradient id="headerGradient" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" style="stop-color:#8B7BD8;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#A78BFA;stop-opacity:1" />
    </linearGradient>
    <style>
      .bg { fill: #0f1419; }
      .title { fill: #ffffff; font-family: 'Arial', sans-serif; font-size: 64px; font-weight: bold; }
      .label { fill: #a0aec0; font-family: 'Arial', sans-serif; font-size: 32px; }
      .value { fill: #ffffff; font-family: 'Arial', sans-serif; font-size: 36px; font-weight: bold; }
      .box { fill: #2d3748; stroke: #8B7BD8; stroke-width: 2; rx: 10; }
      .url { fill: #A78BFA; font-family: 'Arial', sans-serif; font-size: 32px; text-decoration: underline; }
      .step { fill: #ffd700; font-family: 'Arial', sans-serif; font-size: 28px; font-weight: bold; }
      .copy-btn { cursor: pointer; }
      .copy-btn:hover rect { fill: #8B7BD8; }
    </style>
  </defs>
  
  <!-- Background -->
  <rect class="bg" width="1920" height="1080"/>
  
  <!-- Header bar -->
  <rect x="0" y="0" width="1920" height="120" fill="url(#headerGradient)"/>
  
  <!-- Title -->
  <text class="title" x="960" y="80" text-anchor="middle">Amazon WorkSpaces 接続情報</text>
  
  <!-- Connection URL and Steps Section -->
  <rect class="box" x="200" y="200" width="1520" height="220"/>
  <text class="label" x="240" y="250">接続URL:</text>
  <text class="url" x="240" y="295">https://clients.amazonworkspaces.com/</text>
  <text class="step" x="240" y="350">1. Web Access を選択</text>
  <text class="step" x="240" y="390">2. 「Launch」をクリック</text>
  
  <!-- Copy button for URL -->
  <g class="copy-btn" onclick="copyToClipboard('https://clients.amazonworkspaces.com/')">
    <rect x="1500" y="240" width="180" height="60" rx="8" fill="#1a202c" stroke="#8B7BD8" stroke-width="2"/>
    <text x="1590" y="275" text-anchor="middle" fill="#ffffff" font-family="Arial, sans-serif" font-size="24" font-weight="bold">
      📋 コピー
    </text>
  </g>
  
  <!-- Registration Code Section -->
  <rect class="box" x="200" y="460" width="1520" height="100"/>
  <text class="label" x="240" y="515">登録コード:</text>
  <text class="value" x="500" y="515">XXXX</text>
  
  <!-- Copy button for Registration Code -->
  <g class="copy-btn" onclick="copyToClipboard('XXXX')">
    <rect x="1500" y="480" width="180" height="60" rx="8" fill="#1a202c" stroke="#8B7BD8" stroke-width="2"/>
    <text x="1590" y="515" text-anchor="middle" fill="#ffffff" font-family="Arial, sans-serif" font-size="24" font-weight="bold">
      📋 コピー
    </text>
  </g>
  
  <!-- Username Section -->
  <rect class="box" x="200" y="600" width="1520" height="100"/>
  <text class="label" x="240" y="655">ユーザ名:</text>
  <text class="value" x="500" y="655">seminar-user-XX</text>
  <text class="label" x="900" y="655" font-size="28px">(座席番号)</text>
  
  <!-- Copy button for Username -->
  <g class="copy-btn" onclick="copyToClipboard('seminar-user-XX')">
    <rect x="1500" y="620" width="180" height="60" rx="8" fill="#1a202c" stroke="#8B7BD8" stroke-width="2"/>
    <text x="1590" y="655" text-anchor="middle" fill="#ffffff" font-family="Arial, sans-serif" font-size="24" font-weight="bold">
      📋 コピー
    </text>
  </g>
  
  <!-- Password Section -->
  <rect class="box" x="200" y="740" width="1520" height="100"/>
  <text class="label" x="240" y="795">パスワード:</text>
  <text class="value" x="500" y="795">Networld2!</text>
  
  <!-- Copy button for Password -->
  <g class="copy-btn" onclick="copyToClipboard('Networld2!')">
    <rect x="1500" y="760" width="180" height="60" rx="8" fill="#1a202c" stroke="#8B7BD8" stroke-width="2"/>
    <text x="1590" y="795" text-anchor="middle" fill="#ffffff" font-family="Arial, sans-serif" font-size="24" font-weight="bold">
      📋 コピー
    </text>
  </g>
  
  <!-- Footer Note -->
  <text class="label" x="960" y="910" text-anchor="middle" font-size="28px">※ 接続に問題がある場合は、スタッフまでお声がけください</text>
  
  <script type="text/javascript">
    <![CDATA[
    function copyToClipboard(text) {
      if (navigator.clipboard && window.isSecureContext) {
        navigator.clipboard.writeText(text).then(function() {
          showCopyMessage();
        }).catch(function(err) {
          console.error('コピーに失敗しました: ', err);
        });
      } else {
        // フォールバック: テキストエリアを使用
        const textArea = document.createElement('textarea');
        textArea.value = text;
        document.body.appendChild(textArea);
        textArea.select();
        try {
          document.execCommand('copy');
          showCopyMessage();
        } catch (err) {
          console.error('コピーに失敗しました: ', err);
        }
        document.body.removeChild(textArea);
      }
    }
    
    function showCopyMessage() {
      // 既存のメッセージを削除
      const existingMessage = document.getElementById('copyMessage');
      if (existingMessage) {
        existingMessage.remove();
      }
      
      // コピー完了メッセージを表示
      const message = document.createElementNS('http://www.w3.org/2000/svg', 'g');
      message.setAttribute('id', 'copyMessage');
      
      const rect = document.createElementNS('http://www.w3.org/2000/svg', 'rect');
      rect.setAttribute('x', '810');
      rect.setAttribute('y', '500');
      rect.setAttribute('width', '300');
      rect.setAttribute('height', '80');
      rect.setAttribute('rx', '15');
      rect.setAttribute('fill', '#32cd32');
      rect.setAttribute('opacity', '0.95');
      
      const text = document.createElementNS('http://www.w3.org/2000/svg', 'text');
      text.setAttribute('x', '960');
      text.setAttribute('y', '550');
      text.setAttribute('text-anchor', 'middle');
      text.setAttribute('fill', '#ffffff');
      text.setAttribute('font-family', 'Arial, sans-serif');
      text.setAttribute('font-size', '32');
      text.setAttribute('font-weight', 'bold');
      text.textContent = '✓ コピーしました！';
      
      message.appendChild(rect);
      message.appendChild(text);
      document.querySelector('svg').appendChild(message);
      
      // 2秒後にメッセージを削除
      setTimeout(function() {
        const msg = document.getElementById('copyMessage');
        if (msg) {
          msg.remove();
        }
      }, 2000);
    }
    ]]>
  </script>
  
</svg>
